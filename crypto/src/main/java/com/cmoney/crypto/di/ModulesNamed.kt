package com.cmoney.crypto.di

import org.koin.core.qualifier.named


val RSA_SHAREDPREFERENCES_NAMED = named("rsa_sharedpreferences_named")

val AES_SHAREDPREFERENCES_NAMED = named("aes_sharedpreferences_named")

val HYBRID_SHAREDPREFERENCES_NAMED = named("hybrid_sharedpreferences_named")

val RSA_KEY_SOURCE_NAMED = named("rsa_key_source_named")

val AES_KEY_SOURCE_NAMED = named("aes_aes_source_named")

val HYBRID_KEY_SOURCE_NAMED = named("hybrid_key_source_named")